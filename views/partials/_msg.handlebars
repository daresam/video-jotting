{{#if success_msg}}
    <div class="alert alert-success text-center my-2">{{success_msg}}</div>
{{/if}}

{{#if error_msg}}
    <div class="alert alert-danger text-center my-2">{{error_msg}}</div>
{{/if}}

{{#if error}}
    <div class="alert alert-danger text-center my-2">{{error}}</div>
{{/if}}